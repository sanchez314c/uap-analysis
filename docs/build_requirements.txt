# Build Requirements for macOS App
# Install these before building the macOS application

# Core build tools
pyinstaller>=5.0.0
py2app>=0.28.0

# Image processing for icon creation
Pillow>=9.0.0

# Additional dependencies for app building
setuptools>=60.0.0
wheel>=0.37.0

# Optional: Code signing and notarization tools
# altool (comes with Xcode Command Line Tools)
# codesign (comes with Xcode Command Line Tools)